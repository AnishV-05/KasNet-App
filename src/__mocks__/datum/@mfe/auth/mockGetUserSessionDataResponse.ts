// import type { UserSession } from '@mfe/auth'

const mockGetUserSessionDataResponse: any = {
  accessToken: 'the_token_mock',
  expirationTime: 3600,
  idToken: 'the_id_mock',
  origin: 'cognito',
  refreshToken: 'the_refresh_mock',
}

export default mockGetUserSessionDataResponse
